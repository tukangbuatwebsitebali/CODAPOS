# Server
APP_PORT=8080
APP_ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=dewata_pos
DB_SSLMODE=disable

# JWT
JWT_SECRET=dewata-pos-secret-key-change-in-production
JWT_EXPIRY_HOURS=24

# Redis (future)
REDIS_HOST=localhost
REDIS_PORT=6379
